bool areRotations(string s1,string s2)
    {
        if(s1.size()!=s2.size()){
            return false;
        }
        s1 = s1 + s1;
        int found = s1.find(s2);
        if(found < s1.size()){
            return true;
        }
        return false;
    }
